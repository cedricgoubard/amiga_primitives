# Example configuration file for any class inheriting from amiga.drivers.zmq.ZMQBackend

zmq:
  class_name: null  # The backend class to use; must be imported in __main__.py
  host: localhost
  port: 5555
  async_client: false  # Whether to use an asynchronous client; true for cameras, false for robots


# Other options specific to the backend
# ...
